# @package _global_

defaults:
  - override /tokenizer: char
  - override /sampling: default
  - override /data: add_1-3digit
  - override /training: exp1
  - override /wandb: default
  - override /model: transformer
  - _self_

data:
  format:
    pad: $
    reverse_ans: true
    encdec: true

wandb:
  project: "addition-1-3-digit"
  run_name: "transformer"